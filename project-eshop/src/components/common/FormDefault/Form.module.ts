import styled, { css } from 'styled-components';
import { AppType } from 'types/AppType';

export const FormContainerDefault = styled.form<AppType>`
  ${({ theme, ...props }) => css`
    width: 100%;
    display: flex;
    justify-content: center;
    flex-direction: column;

    /* Padding Default */

    /* Margin Default */

    /*UTILS DEFAULT */

    /*BORDER DEFAULT */

    /*BORDER RADIUS DEFAULT */

    /*HEIGHT DEFAULT  */

    /*WIDTH DEFAULT  */

    /*EFFECT DEFAULT */

    /*BEFORE DEFAULT  */

    /*BEFORE DEFAULT  */

    /*POSITION DEFAULT  */

    /* GRID DEFAULT */

    /* FLEX DEFAULT */
  `}
`;
